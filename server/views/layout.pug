doctype html
html
  head
    title= `${title ? title : helpers.siteName}`
    link(rel='stylesheet', href='/build/style.min.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    .app
      
      block content
    block scripts
      script(src="/build/bundle.min.js")  
